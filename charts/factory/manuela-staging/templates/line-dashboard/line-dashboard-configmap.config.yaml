apiVersion: v1
kind: ConfigMap
metadata:
  name: line-dashboard-configmap
  namespace: manuela-staging-line-dashboard
  labels:
    app.kubernetes.io/instance: manuela-staging
data:
  "config.json": |-
    {
      "websocketHost": "http://messaging-manuela-staging-messaging.{{ .Values.global.localClusterDomain }}",
      "websocketPath": "/api/service-web/socket",
      "SERVER_TIMEOUT": "20000"
    }
